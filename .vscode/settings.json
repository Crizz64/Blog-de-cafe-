{
  "liveServer.settings.customBrowser": "firefox:PrivateMode",
  "liveServer.settings.port": 3000,
  // The MIME type for ".js" files is set to "application/javascript" to ensure compatibility with certain browsers or server configurations.
  // This setting is not recognized by Live Server and can be managed directly in server configurations if needed.
}

